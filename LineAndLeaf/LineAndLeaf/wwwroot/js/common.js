hideLoading();
function showLoading() {
    $('#loadingOverlay').fadeIn(200); // Or .show() for instant display
}

function hideLoading() {
    $('#loadingOverlay').fadeOut(200); // Or .hide() for instant hide
}
